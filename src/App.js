import "./App.css";

import React from "react";
import CounterContainer from "./container/counterContainer";

const App = (props) => {
  return (
    <div>
      <CounterContainer />
    </div>
  );
};

export default App;
